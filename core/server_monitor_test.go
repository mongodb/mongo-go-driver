package core_test

import (
	"testing"
)

func TestServerMonitor(t *testing.T) {
	// if testing.Short() {
	// 	t.Skip("skipping server monitor test because it is long")
	// }

	// ep := NewNetEndpoint("tcp", *host)
	// codec := msg.NewWireProtocolCodec()
	// subject := NewServerMonitor(ep, codec, time.Duration(2000)*time.Millisecond)

	// for d := range subject.C {
	// 	fmt.Printf("%s:\n %# v", time.Now(), pretty.Formatter(d))
	// }
}
